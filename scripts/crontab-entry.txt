# Claude Code Slack Bot Health Check
# Run every 5 minutes to check if bot is healthy and restart if needed
# NOTE: Replace BOT_INSTALL_PATH with your actual bot installation path
*/5 * * * * BOT_INSTALL_PATH/scripts/health-check-simple.sh

# Optional: Clean up logs daily at 2 AM
0 2 * * * find BOT_INSTALL_PATH/logs -name "*.log" -mtime +7 -delete